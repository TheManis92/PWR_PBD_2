{% extends 'base.html.twig' %}

{% block content %}
    <div class="container">
        {{ form_start(form) }}

        {{ form_widget(form) }}
        <div>
            <button id="btn" class="btn btn-lg btn-primary" type="submit">Update</button>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock content %}